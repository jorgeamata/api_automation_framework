package com.projectName.basicTestScripts;

import org.testng.annotations.BeforeClass;

import io.restassured.RestAssured;

public class Test015_BeforeClass {
	
	/*
	 * @BeforeClass public void setup() { RestAssured.baseURI = "";
	 * RestAssured.basePath = ""; RestAssured.port = 8080;
	 * RestAssured.authentication = basic ("username", "password");
	 * RestAssured.rootPath = "x.y.z"; RestAssured.filters("") //List of default
	 * filters RestAssured.requestSpecification = //Default request specification
	 * RestAssured.responseSpecification = //Default response specification
	 * RestAssured.urlEncodingEnabled = //specify if rest assured should URL
	 * encoding the parameters RestAssured.defaultParser = //specify a parser for a
	 * given content type RestAssured.unregisterParser(); //unregister a parser for
	 * the given content type RestAssured.useRelaxedHTTPSValidation(null); //in case
	 * server using invalid certificate and throwing SSLPeerUnverified
	 * RestAssured.trustStore(KEYSTORE); //For java keystore file
	 * RestAssured.reset(); //reset all configuration to default like
	 * baseURI(localhost), basePath (empty) }
	 */

}
